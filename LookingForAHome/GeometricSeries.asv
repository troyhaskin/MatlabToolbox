function Series = GeometricSeries(Scale,Ratio,Nterms)
% GeometricSeries()
%   
%   Returns a vector with each each element being a ter in a geometric series.
%   A geometric sequence "G" with "n" terms, scale factor "a", and term ratio "r" has the 
%   form:
%       G = {a , a*r , a*r^2 ,..., a*r^n} = a {1 , r , r^2 ,..., r^n}
%   

    Series = ones(Nterms,1);
    
    for k = 2:Nterms
        Series(k) =  Series(k-1) + Ratio^(k-1);
    end
    
    Series = Scale * Series;

end